# Database — Neon (https://neon.tech)
# Copy the values from your Neon project dashboard → Connection Details
PGHOST=                   # pooled host  (e.g. ep-xxx-pooler.region.aws.neon.tech)
PGHOST_UNPOOLED=          # direct host  (e.g. ep-xxx.region.aws.neon.tech)
PGUSER=
PGDATABASE=
PGPASSWORD=

# Auth — generate with: openssl rand -base64 32
AUTH_SECRET=
